{
  "name": "@tm/lib.client.user",
  "version": "1.0.0",
  "description": "API client",
  "main": "bin/index.js",
  "typings": "bin/index",
  "scripts": {
    "clean": "rimraf bin/*",
    "build": "yarn run clean && yarn && tsc",
    "b": "yarn run build",
    "prepublishOnly": "yarn version patch && yarn run build",
    "p": "yarn publish --access public"
  },
  "keywords": [],
  "author": "MyBestBook.NET",
  "license": "UNLICENSED",
  "repository": {
    "url": "https://github.com/thanhptr/tm.lib.client.user.git"
  },
  "devDependencies": {
    "@types/node": "^9.4.0",
    "typescript": "^2.7.1"
  },
  "dependencies": {
    "@tm/lib.client": "thanhptr/tm.lib.client",
    "inversify": "^4.9.0",
    "rimraf": "^2.6.2"
  }
}
